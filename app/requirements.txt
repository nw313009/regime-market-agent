# Databricks App (Streamlit) dependencies — consumed automatically by the app at deploy time.
#
# Scope: only what the three pages need to render. The app has no SparkSession and does no
# model fitting, so statsmodels, numpy and exchange_calendars are deliberately absent — the app
# reads Gold numbers that the pipeline already computed.
#
# This file is separate from the repository-root requirements.txt (local 3.12 venv, fully
# pinned) and from requirements-databricks.txt (cluster installs). Never pip-upgrade pandas,
# numpy or pyarrow on a Databricks cluster; the runtime pins them against its Spark build.

streamlit==1.61.1

# Delta reads over the serverless SQL warehouse declared in app/app.yaml.
databricks-sql-connector==4.4.0

# Lakebase (Postgres): watchlist and saved reports.
psycopg2-binary==2.9.12

# config/config.yaml loading.
PyYAML==6.0.3

# Model serving calls made through src/llm/call_model.py.
requests==2.34.2
